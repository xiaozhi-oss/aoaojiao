<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaozhi.aoaojiao.mapper.SysRoleMapper">

    <resultMap type="com.xiaozhi.aoaojiao.model.entity.SysRole" id="SysRoleResult">
        <id     property="roleId"       column="role_id"      />
        <result property="roleName"     column="role_name"      />
        <result property="roleStr"      column="role_str"     />
        <result property="roleSort"     column="role_sort"     />
        <result property="remark"       column="remark"        />
        <result property="defFalg"      column="def_falg" />
        <result property="createTime"   column="create_time"          />
        <result property="createBy"     column="create_by"       />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"       />
        <result property="status"       column="status"     />
        <collection  property="menus"   javaType="java.util.List"   resultMap="menuResult"      />
    </resultMap>

    <resultMap id="menuResult" type="com.xiaozhi.aoaojiao.model.entity.SysMenu">
        <id     property="menuId"       column="menu_id"        />
        <result property="menuName"     column="menu_name"      />
    </resultMap>

    <sql id="selectSysRoleSql">
        SELECT r.role_id, r.role_name, r.role_str, r.role_sort, r.remark, r.def_flag, r.create_time,
        r.create_by, r.update_by, r.update_time, r.`status`,
        m.menu_id, m.menu_name
        FROM sys_role r
        LEFT JOIN sys_role_menu rm ON r.role_id = rm.role_id
        LEFT JOIN sys_menu m ON rm.menu_id = m.menu_id
    </sql>

    <select id="selectRoleList" parameterType="com.xiaozhi.aoaojiao.model.entity.SysRole" resultMap="SysRoleResult">
        <include refid="selectSysRoleSql" />
        <where>
            <if test="roleName != null">
                r.role_name = #{roleName}
            </if>
            <if test="roleStr != null">
                AND r.role_str = #{roleStr}
            </if>
            <if test="status != null">
                AND r.status = #{status}
            </if>
            AND r.def_flag=0
        </where>
    </select>

</mapper>
